<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gachon.mpclass.brainstorm">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Brainstorm">
        <activity android:name=".SignupActivity"></activity>
        <activity android:name=".Mindmap"></activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".EnterActivity" />
        <activity android:name=".Chatting" />
        <activity android:name=".MakeRoomActivity"/>
        <activity android:name=".ChatAdapter"/>
        <activity android:name=".MessageItem"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>